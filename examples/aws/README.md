# AWS example

```
aws/
└── infra/     # Deploy Vault Enterprise on AWS
```

See [`infra/`](./infra/) for steps, outputs, and barebones mode documentation.

After deploying Vault, use [`examples/dynamic/`](../dynamic/) to configure TFE dynamic credential flows — it works with Vault on AWS or Azure.

